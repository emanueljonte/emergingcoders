score = int(input("The grade:"))
if score > 89:
    print("A")

else:
    
    if score > 79:
        print("B")
    else:

        if score > 69:
            print("C")

        else:
    
            if score > 59:
                print("D")

            else:
                print("F")
